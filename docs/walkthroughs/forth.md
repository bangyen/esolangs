# Forþ walkthrough

The example shows stack evaluation: tokens push values or invoke words, and
control constructs consume the condition they test. Its terminating loop has
a monotone counter; inspect the matching interpreter test for exact stack
states.
