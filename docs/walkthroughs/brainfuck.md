# Brainfuck walkthrough

The example is a tape walk: `+` and `-` change the current cell, `>` and
`<` move, and brackets repeat while the current cell is nonzero. Its loop
terminates because its counter decreases to zero.
