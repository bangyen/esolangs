# Minifuck Boolean generator

Minifuck's generated programs embed each input once at equal width.

The sculpted route separates rows through pointer displacement and is the general fallback; it is a construction, not an unbounded search.
