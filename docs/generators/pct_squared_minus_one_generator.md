# `%^2^-1` boolean generator

This parameterized generator embeds each input once.

Six constructions cover the cheap shapes.

The remaining research path is to interleave embedding and folding so rows do not all need to be separated at once.
